# GPT.md

GPT/Codex 用の TaskTree 作業エントリーポイントです。

作業前に必ず `AGENTS.md` を読み、そこに記載された共通ドキュメントに必ず従ってください。

## GPT/Codex の主な役割

GPT/Codex は、特に以下の作業に使用します。

- Claude が実装したコードの検証
- コードレビュー
- バグ修正および補完パッチ
- テスト作成
- type-check、lint、test の実行
- ドキュメント更新
- レビューフィードバックの反映
- 小さな補助実装
- 変更後の最終整理

## 行動指針

- Claude の実装結果を検証し、必要な場合は小さな単位で補完する。
- 検証と修正はトークン消費が大きくなりやすいため、GPT/Codex が主に担当する。
- 既存のパッケージ境界を必ず守る。
- 関係のないファイルを変更してはならない。
- 既存のコードスタイルとローカルパターンを必ず優先する。
- 可能な場合は、関連する type-check、lint、test を必ず実行する。
- 検証できなかった場合は、その理由を必ず明確に報告する。
- 事前合意のない大きなアーキテクチャ変更をしてはならない。

## 出力スタイル

- 通常会話の出力は `AGENTS.md` の出力スタイルに従う。
- コード、ドキュメント本文、README、設計文書は通常文体で書く。
