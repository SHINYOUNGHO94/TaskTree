# CLAUDE.md

Claude 用の TaskTree 作業エントリーポイントです。

作業前に必ず `AGENTS.md` を読み、そこに記載された共通ドキュメントに必ず従ってください。

## Claude の主な役割

Claude は、特に以下の作業に使用します。

- 主要機能の実装
- 複雑なコード修正
- 既存コードの流れを踏まえたリファクタリング
- 長いコンテキストを前提としたコード理解
- 要件整理
- UX フローの確認
- 大きな変更前の実装計画作成
- 隠れたエッジケースの発見

## 行動指針

- `docs/core` の共通ルールを上書きしてはならない。
- 実装前に、曖昧な要件を必ず明確にする。
- 大きな変更では、必ず影響範囲と計画を先に提示する。
- 「今必ず必要な変更」と「将来的に望ましい改善」を必ず分けて説明する。
- 機能実装では、既存のパッケージ境界と実装スタイルを必ず優先する。
- Claude はトークン消費が大きいため、不要な広範囲探索を避け、実装に必要な核心ファイルを優先して読む。
- 実装結果は GPT/Codex が検証・補完できるように、変更意図と確認ポイントを必ず残す。

## Claude Agent Teams の使用基準

Claude Agent Teams は、複数の Claude Code セッションをチームとして動かすための機能です。

小さな作業では使用してはなりません。通常の Claude Code セッションで対応します。

Agent Teams を使用してよい作業:

- `task-app`、`task-core`、`task-api`、`task-infra` など複数パッケージにまたがる変更
- 認証、組織階層、権限、DynamoDB 構造に関係する大きな変更
- フロントエンド、API、テストを並行して調査する必要がある作業
- 複数の仮説を並行して検証するデバッグ
- 大きなコードレビューや実装方針比較

Agent Teams を使用してはならない作業:

- README または docs の小さな修正
- 単一ファイルの小さな修正
- 小さな UI 文言修正
- 明確なバグの一点修正

推奨運用:

1. まず Agent Teams で影響範囲の調査と実装計画を行う。
2. 実装前に、チームリードが計画とリスクをまとめる。
3. 同じファイルを複数 teammate が同時に編集しないように、担当範囲を必ず分ける。
4. 実装後は GPT/Codex による外部検証を必ず行う。

## 出力スタイル

- 通常会話の出力は `AGENTS.md` の出力スタイルに従う。
- コード、ドキュメント本文、README、設計文書は通常文体で書く。
